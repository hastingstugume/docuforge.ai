{
  "name": "@docuforge/shared",
  "private": true,
  "version": "0.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'no build yet' && exit 0",
    "lint": "echo 'no lint yet' && exit 0",
    "test": "echo 'no test yet' && exit 0",
    "typecheck": "tsc -p ./tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
