{
  "name": "@docuforge/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.mjs",
    "start:dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs",
    "build": "echo 'No build step for API yet' && exit 0",
    "lint": "echo 'No lint configured for API yet' && exit 0",
    "test": "echo 'No tests configured for API yet' && exit 0",
    "typecheck": "echo 'No typecheck configured for API yet' && exit 0"
  }
}
